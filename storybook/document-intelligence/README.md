# 文档智能 Storybook

本目录收录文档、图片、音视频的理解、抽取、转换、生成和复核的必过回归 Case。

所有 Case 使用合成或脱敏夹具，在私有验证环境中执行；公开文档仅保留输入类型、验收断言、证据要求和隐私边界。任何 `required` Case 在一次迭代中处于 `fail`、`blocked` 或 `not run`，均不能视为通过。

本域覆盖的典型链路包括：多模态解析、结构化抽取、表格转换、知识构建、跨文件比对、总结核验、内容生成、文件治理与可追溯检索。
